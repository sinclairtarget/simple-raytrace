#ifndef GRAPHICS_H
#define GRAPHICS_H

// The size of a rectangle.
typedef struct {
    int width;
    int height;
} RectSize;

// An RGB color.
typedef struct {
    float r;
    float g;
    float b;
    float a;
} Color;

#endif
